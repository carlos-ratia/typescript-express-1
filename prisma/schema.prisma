generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DSN")
}

model Brand {
  id   String @id @default(uuid())
  name String
}

model SequelizeMeta {
  name String @id
}
